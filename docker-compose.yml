services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: hdr-datasets:latest
    pull_policy: never
    container_name: hdr-datasets-app
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
